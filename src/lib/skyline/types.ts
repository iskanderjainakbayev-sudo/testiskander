export type DashSave = { bestScore: number; crystals: number; sound: number };
export type DashSnapshot = { score: number; crystals: number; health: number; speed: number; isJumping: boolean; policeDistance: number; gameOver: boolean };
